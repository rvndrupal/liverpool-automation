1.-instalar node
2.-instalar playwright js  

npx playwright test liverpool.spec --headed
npx playwright test tests/liverpool.spec.js --headed
npx playwright test tests/nintendo-switch.spec.js --headed
npx playwright test tests/nintendo-switch-api.spec.js --headed
npx playwright test tests/liverpool-api.spec --headed
npx playwright test tests/liverpool-api.spec --headed --reporter=list   //Reporte url 

#### Instalar para word ######
npm install docx


#### Repo github #####
git init
git add .
git commit -m "feat: initial Liverpool E2E automation suite"
git branch -M main
git remote add origin https://github.com/rvndrupal/liverpool-automation.git
git push -u origin main

#### GIT ACTIONS #######
git add .github/workflows/test.yml
git commit -m "Se agrega GitHub Actions"
git push

git add pages/SearchPage.js tests/liverpool.spec.js tests/liverpool-api.spec.js
git commit -m "Quitando el anti-Bot"
git push

git add .
git commit -m "Se crean Archivos Readme.md"
git push



npx playwright test — Corre todos los tests
npx playwright test --headed — Corre con browser visible
npx playwright test nombre.spec.js — Corre un archivo específico
npx playwright test --ui — Abre la UI interactiva de Playwright
npx playwright show-report — Abre el reporte HTML del último run
npx playwright codegen https://url.com — Graba tus acciones y genera código

